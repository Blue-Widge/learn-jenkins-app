pipeline {
    agent any
    stages {
        stage('MainStage') {
            steps 
            {
                sh '''
                    npm --version
                '''
            }
        }
    }
}
